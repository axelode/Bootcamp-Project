<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>Project Detail</title>
</head>
<body>
    
    <!-- navbar -->
    <nav id="nav" class="navbar navbar-expand-lg navbar-light bg-light px-3 fixed-top shadow"></nav>

    <!-- main page -->
    <div class="container-fluid d-grid justify-content-center mt-5 py-5" style="background-color: #e3e7eb;">
        <h3 class="text-center fw-bold mb-3 mb-xl-5">{{ dataSelected.project_name }}</h3>
        <div class="box-head d-grid d-xl-flex gap-3 gap-xl-5">
            <div class="rounded overflow-hidden" style="width: 100%;">
                <img
                    src="{{ dataSelected.image }}"
                    alt=""
                    width="100%"
                />
            </div>
            <div class="box-right d-grid d-xl-flex flex-xl-column justify-content-xl-start gap-3 gap-xl-3 justify-content-between" style="width: 100%;">
                <div class="time-box">
                    <h5 class="fw-bold">Durations</h5>
                    <div class="d-flex align-items-center gap-2">
                        <img src="/assets/icons/calender.svg" alt="" width="20px">
                        <span>{{ newStartDateFormat }}  - {{ newEndDateFormat }}</span>
                    </div>
                    
                    <div class="d-flex align-items-center gap-2">
                        <img src="/assets/icons/clock.svg" alt="" width="20px">
                        <span>{{ dataSelected.duration }}</span>
                    </div>
                </div>
                <div class="tech">
                    <h5 class="fw-bold">Technologies</h5>
                    <div id="tech-box-icon" class="tech-box-icon">
                        {{#each dataIcons}}
                            {{{this}}}
                        {{/each}}
                    </div>
                </div>
            </div>
        </div>
        <div class="box-body mt-3">
            <p>{{ dataSelected.description }}</p>

            <div class="d-flex justify-content-center mt-5">
                <a href="/">
                    <button class="btn"><img src="/assets/icons/close.svg" alt="" width="50px"></button>
                </a>
            </div>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
    <script src="/assets/js/navbar.js"></script>

</body>
</html>